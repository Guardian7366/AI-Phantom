# configs/maze_evaluation.yaml
seed: 123
device: "auto"

env:
  size: 8
  wall_prob: 0.28
  max_gen_tries: 200
  max_steps: 128

  step_penalty: -0.01
  invalid_move_penalty: -0.02
  goal_reward: 1.0
  progress_reward: 0.02

eval:
  episodes: 500
  curriculum_level: 2
